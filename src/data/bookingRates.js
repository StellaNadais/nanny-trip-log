/** Default hourly rate for one child. */
export const HOURLY_RATE = 31

/** Added to hourly rate for each child beyond the first. */
export const EXTRA_CHILD_PER_HOUR = 5

/** Flat rate per overnight at the family's house (calendar nights between start and end dates). */
export const OVERNIGHT_RATE = 160
