import EventsBoardPanel from './EventsBoardPanel'

/** Events tab content — list board (nanny Events tab + parent /book). */
export default function EventsTilesSection() {
  return <EventsBoardPanel />
}
